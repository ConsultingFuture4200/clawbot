---
phase: 03-drafts-delegation
plan: 04
type: execute
wave: 3
depends_on: ["03-03"]
files_modified: []
autonomous: false
requirements: [DRAFT-01, DRAFT-02, DRAFT-03, DRAFT-04, DRAFT-05, DRAFT-06, DRAFT-07, DELEG-01, DELEG-02, DELEG-03, DELEG-04, DELEG-05, DELEG-06, DELEG-07, DELEG-08, DELEG-09]

must_haves:
  truths:
    - "All modules load without errors"
    - "MIME messages contain proper threading headers"
    - "Draft tracker state operations work correctly"
    - "Delegation context contains only metadata (no full body)"
    - "Inline keyboard callback_data fits within 64-byte limit"
    - "Pipeline entry point accepts all required parameters"
  artifacts: []
  key_links: []
---

<objective>
Verify the complete Phase 3 implementation: all modules load, MIME construction works, draft tracking operates correctly, delegation context is metadata-only, inline keyboards are within Telegram limits, and the pipeline is wired end-to-end.

Purpose: Gate check before marking Phase 3 complete. Catches wiring issues, import errors, and contract violations that unit-level checks in individual plans might miss.
Output: Verification results and human sign-off
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/03-drafts-delegation/03-CONTEXT.md
@.planning/phases/03-drafts-delegation/03-01-SUMMARY.md
@.planning/phases/03-drafts-delegation/03-02-SUMMARY.md
@.planning/phases/03-drafts-delegation/03-03-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Run comprehensive module verification script</name>
  <files></files>
  <read_first>
    - sandbox/skills/classify-email/index.js
    - sandbox/skills/classify-email/types.js
    - sandbox/skills/classify-email/draft-generator.js
    - sandbox/skills/classify-email/draft-templates.js
    - sandbox/skills/classify-email/delegator.js
    - sandbox/skills/classify-email/delivery.js
    - sandbox/skills/classify-email/digest-formatter.js
    - sandbox/state/draft-tracker.json
    - sandbox/state/delegation-queue.json
    - sandbox/config/draft-templates.json
    - config/openclaw.json5
  </read_first>
  <action>
Create and run a verification script (inline, not saved to disk) that checks all Phase 3 requirements:

**Module loading (8 checks):**
1. `require('./sandbox/skills/classify-email/types.js')` loads without error
2. `require('./sandbox/skills/classify-email/draft-templates.js')` loads without error
3. `require('./sandbox/skills/classify-email/draft-generator.js')` loads without error
4. `require('./sandbox/skills/classify-email/delegator.js')` loads without error
5. `require('./sandbox/skills/classify-email/delivery.js')` loads without error
6. `require('./sandbox/skills/classify-email/digest-formatter.js')` loads without error
7. `require('./sandbox/skills/classify-email/index.js')` loads without error
8. All state files (draft-tracker.json, delegation-queue.json) parse as valid JSON

**Types verification (6 checks):**
9. DRAFT_CATEGORIES is `['routine', 'calendar', 'urgent']`
10. DELEGATION_CATEGORIES is `['code', 'calendar', 'research', 'home', 'urgent']`
11. DELEGATION_ROUTING maps code->dev, calendar->productivity, research->research, home->home, urgent->main
12. DRAFT_TTL_HOURS is 48
13. DELEGATION_MAX_RETRIES is 3
14. CLAUDE_MODEL is 'claude-sonnet-4-6'

**MIME verification (3 checks):**
15. buildReplyMime with inReplyTo produces output containing `In-Reply-To:` header
16. buildReplyMime with inReplyTo produces output containing `References:` header
17. buildReplyMime prepends `Re:` to subject when not already present

**Draft templates verification (2 checks):**
18. draft-templates.json has keys: routine_ack, calendar_accept, calendar_decline
19. getDraftText returns null for category 'code' (no draft per D-02)

**Delegation context verification (2 checks):**
20. buildDelegationContext output has keys: sender, subject, snippet, account, threadId, messageId, classification
21. buildDelegationContext output does NOT have key: body (metadata only per D-08)

**Inline keyboard verification (2 checks):**
22. buildDraftApprovalKeyboard returns 3-row keyboard
23. All callback_data values <= 64 bytes

**Pipeline exports verification (4 checks):**
24. index.js exports handleNewEmails (function)
25. index.js exports handleCallbackQuery (function)
26. index.js exports runHeartbeatMaintenance (function)
27. delivery.js exports handleCallbackQuery (function)

**Config verification (2 checks):**
28. openclaw.json5 contains 'agentToAgent'
29. openclaw.json5 contains '"comms"' in allow list

Print results as: `[PASS] description` or `[FAIL] description: reason`. Exit with code = number of failures.
  </action>
  <verify>
    <automated>echo "Verification script runs as part of this task's action"</automated>
  </verify>
  <acceptance_criteria>
    - All 29 checks pass (exit code 0)
    - No module import errors
    - MIME headers present in buildReplyMime output
    - Delegation context is metadata-only (no body field)
    - All callback_data <= 64 bytes
    - openclaw.json5 has agentToAgent with comms in allow list
  </acceptance_criteria>
  <done>All 29 Phase 3 verification checks pass.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Human verification of Phase 3 implementation</name>
  <files></files>
  <action>
Present the Phase 3 implementation for human review. The user needs to verify:

1. Module structure: `ls sandbox/skills/classify-email/` shows all 9 modules (index.js, classifier.js, delivery.js, digest-formatter.js, spam-gate.js, types.js, draft-generator.js, draft-templates.js, delegator.js)
2. State files: `ls sandbox/state/` shows draft-tracker.json and delegation-queue.json alongside existing files
3. Config files: `ls sandbox/config/` shows draft-templates.json
4. MIME threading: run `node -e "const dg = require('./sandbox/skills/classify-email/draft-generator.js'); const mime = dg.buildReplyMime({ to: 'test@example.com', from: 'me@example.com', subject: 'Meeting', body: 'Thanks!', inReplyTo: '<abc@mail.gmail.com>' }); console.log(Buffer.from(mime, 'base64url').toString());"` -- expect MIME with In-Reply-To and References headers
5. Inline keyboard: run `node -e "const df = require('./sandbox/skills/classify-email/digest-formatter.js'); console.log(JSON.stringify(df.buildDraftApprovalKeyboard('r:abc123def456', 't1', 'personal'), null, 2));"` -- expect 3-row keyboard
6. OpenClaw config: `grep -A 5 "agentToAgent" config/openclaw.json5` -- expect enabled:true with comms in allow list
  </action>
  <verify>
    <automated>echo "Human verification checkpoint"</automated>
  </verify>
  <acceptance_criteria>
    - User confirms module structure is correct
    - User confirms MIME threading output looks correct
    - User confirms inline keyboard structure is correct
    - User confirms OpenClaw config has correct agentToAgent settings
  </acceptance_criteria>
  <done>User approves Phase 3 implementation.</done>
</task>

</tasks>

<verification>
All 29 automated checks pass + human visual verification of module structure, MIME output, keyboard layout, and config.
</verification>

<success_criteria>
Phase 3 is verified complete: all modules load, contracts are satisfied, MIME threading works, inline keyboards fit Telegram limits, delegation context is metadata-only, and the pipeline is wired end-to-end.
</success_criteria>

<output>
After completion, create `.planning/phases/03-drafts-delegation/03-04-SUMMARY.md`
</output>
